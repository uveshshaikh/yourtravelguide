# pnpm configuration
# Hoist nothing by default → catches phantom-dependency bugs early (10-year hygiene).
# Packages must declare what they import; we opt-in known framework peers below.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

# Keep the lockfile authoritative in CI.
prefer-frozen-lockfile=true
