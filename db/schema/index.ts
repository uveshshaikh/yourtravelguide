/**
 * Drizzle schema barrel.
 *
 * INTENTIONALLY EMPTY in Sprint 1 — no tables are defined yet (per scope).
 * The knowledge-graph schema (Authority, Source, Evidence, Fact, Claim, …) per
 * the Knowledge Platform Blueprint lands in a later sprint. Every table will be
 * re-exported from here so `db/index.ts` and drizzle-kit pick it up with no
 * config change.
 */
export {};
