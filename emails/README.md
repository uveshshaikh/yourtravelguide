# `emails/` — transactional email templates

Holds email templates (e.g. correction acknowledgements, editor review
notifications) as framework-agnostic components, kept separate from web UI so the
sending provider can change without touching app code.

Empty in Sprint 1 — no emails are sent yet.
