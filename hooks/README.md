# `hooks/` — shared React hooks

Reusable client-side hooks used across more than one feature (e.g. `use-media-query`,
`use-debounced-value`). Feature-specific hooks live inside that feature, not here.

Empty in Sprint 1.
